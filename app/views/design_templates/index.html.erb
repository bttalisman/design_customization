<h1>Templates</h1>

<ul>
<%
@design_templates.each { |t|

  if t.name == '' then
    name = "<none>"
  else
    name = t.name
  end

%>
  <li><%= link_to name, t %></li>
<%
}
%>
</ul>

<%= link_to "New Template", new_design_template_path %>
