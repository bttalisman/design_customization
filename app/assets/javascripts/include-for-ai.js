


function indexOf( a, s )
{
  var foundI = -1;

  for( var index = 0; index < a.length; index++ )
  {
    var item = a[ index ];

    if( item == s )
    {
      foundI = i;
      continue;
    }

  }

  return foundI;
}
